(function(){'use strict';var rows=[].slice.call(document.querySelectorAll('.tier-row')),sheets=[].slice.call(document.querySelectorAll('.sheet'));rows.forEach(function(row,index){if(row.querySelector('.tier-read'))return;var button=document.createElement('button');button.type='button';button.className='tier-read';button.textContent='Read full benefits ↓';button.addEventListener('click',function(){var sheet=sheets[index];if(!sheet)return;sheet.open=true;sheet.scrollIntoView({behavior:'smooth',block:'start'})});row.appendChild(button)});})();
