name: Publish desktop homepage

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/publish-desktop-home.yml'
      - 'desktop.html'
      - 'index.html'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: desktop-home-pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Deploy rebuilt desktop homepage
        id: deployment
        uses: actions/deploy-pages@v4

# Purpose-built desktop homepage, published 2026-08-08.
