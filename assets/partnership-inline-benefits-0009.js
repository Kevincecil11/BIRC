(function(){'use strict';var rows=[].slice.call(document.querySelectorAll('.tier-row')),sheets=[].slice.call(document.querySelectorAll('.sheet'));rows.forEach(function(row,index){var sheet=sheets[index];if(!sheet)return;var old=row.querySelector('.tier-read');if(old)old.remove();var button=document.createElement('button');button.type='button';button.className='tier-read';button.textContent='Read full benefits +';button.setAttribute('aria-expanded','false');var inline=document.createElement('div');inline.className='tier-inline';var body=sheet.querySelector('.sheet-body');inline.innerHTML='<div><div class="tier-inline-grid">'+(body?body.innerHTML:'')+'</div></div>';button.addEventListener('click',function(){var open=!row.classList.contains('open');rows.forEach(function(r){r.classList.remove('open');var b=r.querySelector('.tier-read');if(b){b.textContent='Read full benefits +';b.setAttribute('aria-expanded','false')}});if(open){row.classList.add('open');button.textContent='Show less ×';button.setAttribute('aria-expanded','true')}});row.append(button,inline)});})();
